# SDR Simulator (Streamlit)

Interactive web UI to play with pipeline parameters.

```bash
pip install -r requirements.txt
streamlit run app.py
```

Features:
- Ring buffer simulation (capacity, chunk size, push cycles)
- FIR tap tuning with real-time filter output plot
- Mock IQ generation
- Full pipeline simulation button
